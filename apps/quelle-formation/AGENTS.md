# App Agent Map

This app may be opened directly in Codespaces or through the `oracle-formation`
monorepo.

Public contact email: `info@yann-lephay.com`.

Useful commands from this directory:

```bash
npm run dev
npm run build
npm run lint
```

Before editing, inspect `package.json`, `src/app`, `src/lib/metadata.ts`, and
`src/lib/structured-data.ts`.
